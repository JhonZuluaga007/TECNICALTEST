name: tecnical_test_pragma
description: "A new Flutter project."
# The following line prevents the package from being accidentally published to
# pub.dev using `flutter pub publish`. This is preferred for private packages.
publish_to: 'none' # Remove this line if you wish to publish to pub.dev

# The following defines the version and build number for your application.
# A version number is three numbers separated by dots, like 1.2.43
# followed by an optional build number separated by a +.
# Both the version and the builder number may be overridden in flutter
# build by specifying --build-name and --build-number, respectively.
# In Android, build-name is used as versionName while build-number used as versionCode.
# Read more about Android versioning at https://developer.android.com/studio/publish/versioning
# In iOS, build-name is used as CFBundleShortVersionString while build-number is used as CFBundleVersion.
# Read more about iOS versioning at
# https://developer.apple.com/library/archive/documentation/General/Reference/InfoPlistKeyReference/Articles/CoreFoundationKeys.html
# In Windows, build-name is used as the major, minor, and patch parts
# of the product and file versions while build-number is used as the build suffix.
version: 1.0.0+1

environment:
  sdk: ^3.12.0
  flutter: '>=3.44.0'

# Dependencies specify other packages that your package needs in order to work.
# To automatically upgrade your package dependencies to the latest versions
# consider running `flutter pub upgrade --major-versions`. Alternatively,
# dependencies can be manually updated by changing the version numbers below to
# the latest version available on pub.dev. To see which dependencies have newer
# versions available, run `flutter pub outdated`.
dependencies:
  flutter_screenutil: ^5.9.0
  http: ^1.1.0
  flutter_bloc: ^9.1.1
  # Phase 6. It persists the bloc's `searchHistory`, but it earns its place twice:
  # its `Storage` interface (read/write/delete/clear/close) is a perfectly good
  # key-value store, so the breeds disk cache reuses it instead of pulling in a
  # second persistence package. `core/storage/` wraps it so the data layer never
  # imports this package — it re-exports `package:bloc`, which has no business
  # being visible from `data/`.
  hydrated_bloc: ^11.0.0
  # Only to hand `HydratedStorage.build` a directory, in `main.dart`. It was
  # already here as a transitive dependency of google_fonts; Phase 7 removes that
  # one, and this promotion to a direct dependency is what keeps the storage
  # working when it does.
  path_provider: ^2.1.6
  get_it: ^9.2.1
  injectable: ^2.7.1
  google_fonts: ^8.2.1
  go_router: ^17.4.0
  # `equatable` was here from Phase 2 to Phase 4, described as temporary: it gave
  # value equality to states, events and entities so bloc_test's `expect:` and
  # mocktail's `verify` could work at all. Phase 4 removed it — freezed generates
  # `==`, `hashCode` and `toString` for the same types, plus `copyWith` and
  # `fromJson`, so keeping both would have meant two mechanisms for one job.
  freezed_annotation: ^3.1.0
  json_annotation: ^4.12.0

  flutter:
    sdk: flutter


  # The following adds the Cupertino Icons font to your application.
  # Use with the CupertinoIcons class for iOS style icons.
  cupertino_icons: ^1.0.2

dev_dependencies:
  # Phase 2 removed kiwi_generator and build_runner: flutter_test pins
  # `test_api: 0.7.11` exactly -> the only compatible `test` is 1.31.0 -> which
  # requires `analyzer >=8.0.0 <13.0.0`, while kiwi_generator required
  # `analyzer ^6.0.0`. Empty intersection, so with the generator present adding
  # bloc_test failed resolution.
  #
  # Phase 4 brought build_runner back for freezed, and the conflict was gone
  # because kiwi_generator is what caused it.
  #
  # Phase 5 closed the debt: kiwi is replaced by get_it + injectable, so the
  # registrations are generated again — this time by a generator that resolves
  # alongside everything else. Verified versions: freezed 3.2.5,
  # json_serializable 6.14.1, injectable_generator 2.12.1, build_runner 2.15.1,
  # analyzer 10.2.0.
  #
  # `analyzer` is pinned in this file for a reason; see its own note below.
  build_runner: ^2.15.1
  injectable_generator: ^2.12.1
  freezed: ^3.2.5
  json_serializable: ^6.14.1
  # Pinned below 11 on purpose. Left alone, pub maximizes `analyzer` to 12.x,
  # which no stable freezed supports yet, so it silently resolves freezed to the
  # `3.2.6-dev.1` prerelease. Shipping a prerelease codegen dependency is not
  # worth the newer analyzer, which nothing here uses directly. Remove this pin
  # once a stable freezed supports analyzer 12.
  analyzer: ^10.2.0
  bloc_test: ^10.0.0
  mocktail: ^1.0.4

  flutter_test:
    sdk: flutter

  # The "flutter_lints" package below contains a set of recommended lints to
  # encourage good coding practices. The lint set provided by the package is
  # activated in the `analysis_options.yaml` file located at the root of your
  # package. See that file for information about deactivating specific lint
  # rules and activating additional ones.
  flutter_lints: ^6.0.0

# For information on the generic Dart part of this file, see the
# following page: https://dart.dev/tools/pub/pubspec

# The following section is specific to Flutter packages.
flutter:

  # The following line ensures that the Material Icons font is
  # included with your application, so that you can use the icons in
  # the material Icons class.
  uses-material-design: true

  # To add assets to your application, add an assets section, like this:
  assets:
    - assets/
    - assets/images/

  # An image asset can refer to one or more resolution-specific "variants", see
  # https://flutter.dev/assets-and-images/#resolution-aware

  # For details regarding adding assets from package dependencies, see
  # https://flutter.dev/assets-and-images/#from-packages

  # To add custom fonts to your application, add a fonts section here,
  # in this "flutter" section. Each entry in this list should have a
  # "family" key with the font family name, and a "fonts" key with a
  # list giving the asset and other descriptors for the font. For
  # example:
  # fonts:
  #   - family: Schyler
  #     fonts:
  #       - asset: fonts/Schyler-Regular.ttf
  #       - asset: fonts/Schyler-Italic.ttf
  #         style: italic
  #   - family: Trajan Pro
  #     fonts:
  #       - asset: fonts/TrajanPro.ttf
  #       - asset: fonts/TrajanPro_Bold.ttf
  #         weight: 700
  #
  # For details regarding fonts from package dependencies,
  # see https://flutter.dev/custom-fonts/#from-packages
